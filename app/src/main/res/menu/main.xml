<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single" android:id="@+id/group_sort">
    <item
        android:id="@+id/sortPopularity"
        android:title="By Popularity"
        app:showAsAction="never" />
    <item
        android:id="@+id/sortRating"
        android:title="By Rating"
        app:showAsAction="never" />
    <item
        android:id="@+id/sortRelease"
        android:title="Now playing "
        app:showAsAction="never" />
    </group>
</menu>
